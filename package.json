{
  "name": "hyhq-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "原生微信小程序，无第三方依赖、无需 npm 构建",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=20" }
}
