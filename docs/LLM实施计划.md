# DeepSeek 图像解读与追问：L01–L06

> 本文保留 2026-09-20 初版交付口径。2026-09-21 起，按 [AI 分区互动计划](AI分区互动实施计划.md) 调整为三个板块分别计数、前端隐藏额度、注册统一协议入口与上下文问答；以下初版规则不再代表最新客户端行为。

日期：2026-09-20。开发起始基线：`40740ee`。最初按用户要求先在本机实施；后续已完成真实 DeepSeek 图文联通，并将源码 `26fe544` 部署到北京服务器，服务器保持空 Key、关闭网关。后续微信弹窗补丁未引入后端变更。历史本地验收与后续实际状态分别记录。

## 已确定范围

- 提供方为 DeepSeek，模型 `deepseek-flash`，使用官方 HTTPS Chat Completions 接口；显式关闭深度思考。
- 密钥只由后端环境变量 `DEEPSEEK_API_KEY` 读取；环境总开关与管理配置同时启用且密钥已配置才能请求上游。
- 登录用户每天共最多 **5 个成功回合**，以入队时的北京时间日期归属；跨图片、跨会话、跨花卉和河道入口共享额度。
- 首次解读也占一回合。失败释放用户预留额度，但上游调用尝试及可能产生的 token 成本单独计量；不自动重试结果未知的请求。
- 先绑定已有、本人、未过期的花卉或河道识别任务；本地机器学习结果保持原样，AI 解读单独展示。
- 用户主动同意后选择是否附图；传必要文本和已净化图片，不传 OpenID、令牌或精确位置。原图 24 小时后失效，历史记录存在不等于原图仍可外发。
- 采用独立问答任务进程和轮询；不占用图像推理全局执行锁。无需本地 LLM、Redis 或向量数据库。

## 开发和验收 TODO

- [x] **L01 网关与凭据配置**：固定官方地址/模型、环境读取、无密钥/关闭不外发、非思考、请求与响应大小限制、父/子进程墙钟超时、上游错误脱敏。
- [x] **L02 会话与额度**：北京时间日额度、原子预占/结算、每用户单个进行中任务、请求 ID 幂等及输入冲突检查、全站预算/并发/队列限制。
- [x] **L03 图片与识别上下文**：本人任务与图片校验、uncertain 保留、已发布科普关联、上下文限长、原图过期/删除处理、回写前重新校验。
- [x] **L04 小程序交互**：两个识别结果入口、外发告知、附图选择、发送与轮询、剩余额度、历史会话、删除、会话切换与旧请求隔离。
- [x] **L05 管理与生命周期**：后台开关/限额/预算、只展示密钥配置状态、调用用量及错误统计、删除与注销/过期清理、部署及操作说明。
- [x] **L06 本地验收**：283 项 PostgreSQL 后端测试、214 项前端测试、模拟上游真实 HTTP 联调通过；微信工具验证真实花卉上传/推理、默认不附图、关闭提示、历史入口及确认弹窗失败恢复。
- [x] **L06 外部服务联通验收**：原生微信开发者工具中主动发送文字、附图各一条真实 `deepseek-flash` 请求，两次成功，usage 与账本一致，合计 3131 token；两个会话共用每日额度，验收时已用 2、剩余 3/5。该项覆盖本机图文联通，完整多轮质量、提供方账单、真实微信登录、手机和公网 HTTPS 仍待验收。

## 当前运行与后续维护

- [x] 本机 Key 只存于被 Git 忽略的 `backend/.env`，权限 `0600`；API 与独立 LLM 工作进程已启用。小额手测限制已设为全站每日 10 次提交、100000 token、单次输出最多 600 token、并发 1；每账号每日仍为 5 个成功回合，未重置已消费额度。
- [x] 北京源码 `26fe544` 部署完成，283 项后端回归、71 次核心 HTTP 和 14 次 M2/LLM 关闭检查通过；服务器 Key 为空、网关关闭、LLM 工作进程停用。
- [ ] 手机、公网合法域名与 HTTPS、真实微信登录、完整多轮回答质量和提供方账单核对。
- [ ] 服务器启用真实 LLM 后的运行与资源验收；当前服务器关闭状态不能替代这项。

本轮收尾不扩展 M2-B05 管理统计与审计、M2-B06 模拟批次维护。原生工具版本为 Stable 2.02.2608070、基础库 3.7.12，使用小程序模拟器，未进行手机真机验收。

## 必测行为

1. 同一用户同日第六个成功回合被拒绝；不同入口和新会话不能绕过。
2. 并发请求与重复 request_id 不额外扣额度、不重复外发；幂等键复用不同输入返回冲突。
3. 北京时间跨日、旧日未完成请求完成后的记账日期保持正确。
4. 非本人任务/图片/会话不可访问；后台权限限制有效。
5. 失败任务返还用户回合；上游超时、不完整结果、429、余额不足等有明确状态，未知成本保守记账。
6. 原图过期不再外发；仅结果解读明确缺少图片，不用缩略图静默替换。
7. 删除会话、识别记录或账号时，不恢复已消费额度，不让稍后回写的响应重建私人记录。
8. 不记录密钥、图片 base64 或聊天正文到运行日志；模拟内容不被描述为实测。

## 官方接口依据

- [首次调用](https://api-docs.deepseek.com/zh-cn/)
- [图像理解](https://api-docs.deepseek.com/zh-cn/guides/vision/)
- [Chat Completions 参数](https://api-docs.deepseek.com/zh-cn/api/create-chat-completion/)
- [多轮对话](https://api-docs.deepseek.com/zh-cn/guides/multi_round_chat/)

开发期自动回归不需要真实密钥。上游模拟用于检验本项目业务与错误处理；后续两次真实请求单独记录，不用模拟结果充当实际效果或人民币费用证据。

具体测试数量、复现命令与早期关闭状态见 [LLM 验证记录](verification/LLM网关验证记录.md)；后续见 [真实图文联通记录](verification/DeepSeek真实联通验证记录.md) 与 [北京版本更新记录](verification/北京LLM版本更新记录.md)。额度按平台账号计算；退出重登、删会话和换图片不重置。注销会删除账号关联，重新注册会成为新账号；当前未保留额外身份指纹，全站预算仍统计注销前用量。
