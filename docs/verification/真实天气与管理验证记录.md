# 真实天气与管理验证记录

日期：2026-09-21。部署结果在本记录后续补全。

## 本机验证

- PostgreSQL 隔离测试库：**377 项后端测试通过**。自动测试的 DeepSeek、和风真实凭据均为空，外部服务使用 mock。
- 小程序 Node 测试：**257 项通过**。新增零值/缺失、过期缓存、显式空预警、城市切换竞态、隐藏页面取消更新、服务关闭与错误隔离。
- 现有手测 PostgreSQL 迁移成功，`makemigrations --check --dry-run` 无漂移。
- 实际管理数据库流程 **48 项通过**：日期范围、模型权限裁剪、只读审计、模拟维护预览和天气管理。临时账号/会话/预览全部事务回滚，未删除任何已有观测，未调用外部 API。
- 五个地点的真实天气、空气质量都已通过本机 HTTP 取得。请求再次命中缓存时，数据库请求账本不增加。
- 首次真实空预警发现 `zeroResult=true` 的响应不含 `attributions`，已按官方缺省字段规则修正：只有明确空结果可以缺省，错误响应仍显示不可用。北京、天津、天津师范大学、天津理工大学已返回明确空预警；天津工业大学旧失败仍处于正常冷却，后续自然恢复时再验证，不清空或退款账本。
- 原生微信开发者工具 Stable 2.02.2608070 / 基础库 3.7.12 实际显示天津天气、天津工业大学区域天气、北京 AQI 与暂无预警；选择器列出全部五个地点。真实城市卡与模拟生态观测分开，客户端无额外定位请求。
- 两个实际 Key 均扫描 tracked/index/untracked 和本机日志：未发现泄漏；`backend/.env` 被 Git 忽略且权限 `0600`。

## 月度额度

本机 100 次 + 北京 29,900 次，项目总上限 30,000 次。每次天气、空气、预警分别计一次；失败和超时不退款。持久账本、滚动 31 天附加限制、缓存和断路器在后端执行。严禁恢复旧账本/新建独立带 Key 环境以继续请求。

## 服务器阶段（待执行）

备份现有 `26fe544` 数据库、私有文件、环境与服务配置，再恢复到新数据库、迁移、部署和启用两类网关。服务器将使用新的独立版本目录，保留旧版本用于回退。真实 API 验收只少量请求，保留用量账本。

公网复核：绕过本机代理直接访问域名的 ACME 测试路径，仍收到腾讯云 `webblock.html` 302 拦截；443 未通。代码和内部服务部署不代表域名正式可用，备案接入与 HTTPS/微信合法域名仍待外部条件就绪。
