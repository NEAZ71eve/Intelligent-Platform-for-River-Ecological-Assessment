# 品牌与输入框验证记录

日期：2026-09-21；衔接 [AI 分区互动验证](AI分区互动验证记录.md)。本次按用户要求随上一批本地改动一起提交推送，服务器不更新。

## 变更

- L13：用户提供的主 Logo 放入首页品牌区、个人中心品牌/注册区；保留用户头像。小 Logo 按首页、导览、AI、科普、个人中心及悬浮助手对应，使用同一绿色系。中央 AI 圆更大，底栏保留文字。入口卡和注册卡采用绿色细边框。
- L14：移除所有板块的默认问题和输入框占位提示；初始输入为空。系统提示及文章/地点等服务端上下文仍自动拼接，用户只写自己的问题。同一账号同一会话的未发送草稿可保留，换账号/会话不串入；空白不能提交。
- 来源图保存在 `design/brand/`；运行图片作 JPEG 编码优化，无远程素材请求。主 Logo 原图未裁剪或改绘；子图标用内置 imagegen 按参考图编辑，最终提示词和资产映射见 [品牌素材](../../design/brand/README.md)。

## 检查结果

| 检查 | 结果 |
| --- | --- |
| 小程序完整回归 | **252 / 252 通过**，`.runtime/brand-chat-frontend-final.log` |
| 后端基线 | 上一批 **308 / 308 通过**；本次仅前端与文档修改，未重复跑后端 |
| 差异检查 | `git diff --check` 通过 |
| 本机服务 | API、花卉、河道和 LLM 进程运行，数据库/API 健康 |
| 密钥检查 | 后端 `.env` 被 Git 忽略且权限 0600，扫描已跟踪、待加入文件、index 与本机日志未发现实际 Key |
| 资源体积 | 运行图片合计约 409 KiB，候选小程序资源约 0.98 MB（按排除测试与本地配置的文件字节统计，非微信最终上传包大小） |

六项新增回归覆盖所有入口保持空白、历史/刷新不注入预设问题、草稿保留与身份隔离、隐藏期间已确认发送后清理草稿及空白发送拦截。此前 Markdown、分板块额度和来源上下文回归仍通过。

## 原生页面验收

环境为微信开发者工具 Stable 2.02.2608070 / 基础库 3.7.12 / iPhone 12/13 (Pro) 模拟器。

- 首页可见主 Logo、五个绿色底栏图案及放大的中央 AI 相机图案，图案和标签清晰。
- 实测点击导览图标进入生态导览。针对嵌套图片组件的点击命中问题，底栏改同组件原生 image 视窗，并给整个按钮（含中央凸起）增加明确点击覆盖区域。
- 导览右下浮窗显示绿色 HYHQ 字标；点击字标部分成功进入 AI 页面，位置在底栏上方。
- 开启一个空导览会话后，“有什么想了解的？”下面的输入框无预填内容、无占位文字，计数为 `0 / 500`；模型标识、来源资料和返回入口保留。
- 本轮未发送付费问题；提交前只读核对本机已有 8 条调用账本，最新提交时间为北京时间 01:39:52，均早于本轮界面验收。此前 01:38–01:39 的手测包含 4 条 `LLM_RESPONSE_INCOMPLETE` 失败和 1 条科普成功；本轮未调整网关输出预算，完整长回答仍属于后续质量验收。控制台无 JavaScript 错误，仍有开发工具预加载及旧 `selectable` 属性弃用提示。

手机真机、公网 HTTPS 和正式发布继续保留后续验收。功能提交 `9daceaf` 已成功推送至 `https://github.com/gruTGU/HYHQ` 的 `codex/m2-ecology-experience` 分支；包含前一批 L07–L12 与本次品牌/输入框修改。
