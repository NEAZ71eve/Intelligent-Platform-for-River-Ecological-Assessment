# AI 分区互动验证记录

日期：2026-09-21。起始版本 `c6ea5f6`；本记录对应本机工作区修改，服务器本轮未更新。

## 交付范围

- L07：固定版本 markdown-it 15.0.2 解析模型回复，转换为小程序原生 rich-text 白名单节点。支持标题、强调、列表、引用、代码、表格；链接不发起跳转，远程图片只显示替代文字，原始 HTML 不执行。输入长度、嵌套和节点数有边界，许可证已补齐。
- L08：`recognition`、`explore`、`learn` 三个独立账号日账本，各 5 个成功回合，北京时间零点重置；花卉与河道共用 recognition。新会话、删会话、换来源不清额度。页面不显示常驻额度，发送超出限制才提示当前板块已达上限。
- L09：按板块设置系统提示，由服务端查询公开区域、地点、河湖、文章和路线。正文按字节限长并标记节选；每轮校验公开状态及上下文版本，不能用客户端正文或提示词替换平台资料。模拟指标带来源、时间与模拟标识。
- L10：生态导览、科普智游和相关详情提供右下角 AI 入口；显示 DeepSeek Flash 与对应资料卡片，来源跳转及历史读取使用服务端返回的 ID。
- L11：五项极简底栏、中央加大的圆形 AI 标识；注册/登录处统一提示协议链接，“我的”保留协议入口。移除业务页重复协议勾选与 AI 确认弹窗；附图默认关闭，主动发送、系统权限和破坏性操作确认保留。
- L12：隔离数据库测试、本机迁移、进程重启、原生工具检查与说明更新。

## 自动验证

| 项目 | 结果与证据 |
| --- | --- |
| 完整后端 PostgreSQL | 308 / 308 通过，35.578 秒，`.runtime/llm-scopes-full.log` |
| 完整小程序 Node 测试 | 246 / 246 通过，`.runtime/ai-sections-frontend-final.log` |
| Django 检查 / 迁移漂移 | `check` 无问题；`makemigrations --check --dry-run` 无新增迁移 |
| 差异格式 | `git diff --check` 通过 |

后端覆盖三板块合计 15 回合、第六次拦截、同板块最后额度并发争抢、跨日、删除后记账、旧账本迁移、来源下架/删除/变更、文章正文与模拟来源、异常输入和不能替换系统提示。新增 25 项分区测试；旧缺少逐次同意字段的 HTTP 测试已更新为可创建会话且默认不附图、不自动调用。

前端覆盖安全 Markdown、分区入口、隐藏额度、重复发送与迟到响应隔离、公开来源创建/历史/刷新、底栏高亮时序和注册/上传流程。原生检查发现的“来源卡片创建后丢失标题”和“底栏使用上一页路由导致错位”已补回归修复。

自动测试均清空进程中的真实 Key 并使用模拟上游，不调用付费 API；后端在独立测试数据库运行。

## 本机数据与服务

- 迁移前已生成私有数据库备份：`.runtime/local-test/backups/pre-ai-scopes-20260920T171404Z.dump`，文件 0600、目录 0700，位于 Git 忽略目录。
- 已应用 `llm.0002`。既有 3 条使用账本均保留并归入 recognition；没有清除用户已有消费记录。
- 实际建立 explore/region、learn/content 各一个空会话；只创建会话，不发送问题。只读验证源 ID、板块提示词、当前文章完整正文和图像关闭状态正确，账本仍为 3 条。
- 本机网关保留真实 Key，三板块各 5 成功回合、各 10 次提交保护；全站每日 50 次提交、100000 token、单次输出 600 token、并发 1。打开页面和修改限制不会调用外部服务。
- API、花卉、河道、LLM 四个进程运行，数据库/API 健康检查通过。接口地址 `http://127.0.0.1:8000/api/v1`。
- Key 与来源文件权限均为 0600；后端 `.env` 被 Git 忽略，未进入小程序。精确密钥扫描覆盖已跟踪文件、Git index、未跟踪但可提交文件和本机运行日志，未发现泄露。

## 原生微信开发者工具检查

环境：Stable 2.02.2608070，基础库 3.7.12，iPhone 12/13 (Pro) 模拟器；使用已有开发测试身份，不新建账号或重新接受协议。

- 使用原有真实 DeepSeek 回复检查 Markdown：标题、粗体、无序/有序列表和段落已在原生 rich-text 正确显示。代码、表格及恶意文本边界由自动测试覆盖。
- 生态导览与科普智游的浮动按钮位于底栏上方；详情页位于右下，尺寸收紧后未覆盖底栏。中央 AI 圆完整显示，五项标签保留。
- 开启导览区域会话、从“向日葵类：保留花序与整株信息”文章开启科普会话时，没有重复协议勾选或确认弹窗；不显示日额度。
- 修复后的导览、科普和个人中心底栏高亮与当前页面一致。
- 从“我的 AI 对话”重新打开科普空会话，已正确显示“向日葵类：保留花序与整株信息”和文章摘要；点击“查看相关资料”返回同一篇正文。控制台检查无 JavaScript 错误；开发工具仍有预加载和 `selectable` 属性弃用提示。
- AI 会话显示 DeepSeek Flash，只有主动点击发送才会提交问题；本轮未点击发送，没有新增付费调用。

## 验收边界

本机原生工具和自动测试通过，不代表手机真机、真实微信登录、公网 HTTPS 或完整多轮回答质量已通过。此前 2026-09-20 的真实文字/附图调用仍见 [历史联通记录](DeepSeek真实联通验证记录.md)。服务器仍为此前版本、Key 为空，本轮未提交推送或更新服务器。

实施拆分见 [L07–L12 计划](../AI分区互动实施计划.md)，手测方法见本机 `.runtime/local-test/开始测试.md`。

后续交付补记：本记录中的 AI 分区修改已于同日随品牌与输入框版本 `9daceaf` 提交推送，详见 [后续验证](品牌与输入框验证记录.md)。本记录的测试计数与本地账本数量为当时验收快照。
