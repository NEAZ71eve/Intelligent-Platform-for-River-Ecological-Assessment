# L01–L06：DeepSeek 网关本地验证

日期：2026-09-20。开发起始基线：`40740ee`。本文保留配置真实密钥前的自动回归与关闭状态记录；后续部署和实际调用按下方最新结论及独立报告核对。

## 最新结论（同日后续）

- L01–L05 已实现；L06 本地回归和真实 DeepSeek 文字/附图联通均通过。最新完整回归为后端 **283 项**、前端 **214 项**；新增前端测试覆盖确认按钮平台限制、弹窗失败恢复和重复回调。
- 原生微信开发者工具 Stable 2.02.2608070、基础库 3.7.12 的小程序模拟器完成两次真实 `deepseek-flash` 请求，合计 3131 token，验收时账号已用 2 轮、剩余 3/5。详细耗时、usage 与操作见 [真实联通记录](DeepSeek真实联通验证记录.md)。使用开发模拟账号，未验收手机或真实微信登录。
- 本机 Key 仅保存在被 Git 忽略、权限 `0600` 的后端 `.env`。当前手测配置为全站每日 10 次提交、100000 token、单次输出 600 token、并发 1；每账号每日 5 个成功回合。
- 北京已部署源码 `26fe544`，通过 283 项后端、71 次核心 HTTP、14 次 M2/LLM 关闭检查。服务器 Key 为空、网关关闭、LLM 工作进程停用；后续微信弹窗补丁未引入后端变更。见 [北京更新记录](北京LLM版本更新记录.md)。

## 第一阶段结论（2026-09-20，配置真实密钥前）

- L01–L05 已实现；L06 本地自动回归及本机 HTTP 验证通过。
- 后端 **283 项** PostgreSQL 测试全部通过，包含原有 214 项与本轮新增 69 项；前端 **213 项**全部通过，新增 22 项。
- 数据库迁移无遗漏；本地手工测试库已执行 `llm.0001_initial`，API、花卉、河道、LLM 四个进程运行正常。
- 此阶段未配置真实 `DEEPSEEK_API_KEY`，环境开关关闭。未调用真实 DeepSeek，没有产生此阶段测试的外部 API 费用。
- 此阶段尚未连接或更新北京服务器，systemd 文件当时仅是部署模板；后续部署结果见上方最新结论。

## 实施结果

| 项目 | 行为与验收证据 |
| --- | --- |
| L01 | 固定官方 HTTPS 地址和 `deepseek-flash`；非思考、非流式；Key 仅后端环境；父/子进程独立硬超时；请求和响应大小限制；错误脱敏。Provider 30 项测试包含本地慢响应/卡住传输。 |
| L02 | 北京时间每天最多 5 个成功回合，首问计数；入队预占，失败返还；每账号最多 1 个处理中请求；重复请求 ID 返回同一任务；全站尝试、token、队列和并发限制。含真实 PG 并发抢最后额度及 worker 槽位测试。 |
| L03 | 仅本人有效的已完成识别；默认仅文字；显式附图后发送最大 512 边 JPEG 并去元数据；原图过期不再发送；保留模型不确定性；只选已发布关联科普；最多近 5 个成功历史回合并裁剪文本。 |
| L04 | 花卉/河道结果进入解读；先告知与同意，再建会话，首问需主动发送；显示每日额度、北京时间重置、逐轮图片使用状态；查询可停止/恢复；会话历史分页与删除；账号切换隔离。 |
| L05 | Admin 开关、限额、预算、今日用量和无正文账本；只显示 Key 是否配置；会话、源记录、注销和过期清理联动；晚到回答不重建私人数据。 |

## 自动化验收

环境：macOS、Python 3.12、Django/DRF、真实本机 PostgreSQL（端口 55432），前端 Node 内置测试运行器。测试使用独立临时数据库与媒体目录，不修改 `hyhq_local` 中的手工测试记录。

```bash
# 当前工作区的隔离测试包装器（.runtime 不进入 Git）
HYHQ_LLM_TEST_SUFFIX=suite .venv/bin/python .runtime/run-llm-check.py test --noinput
HYHQ_LLM_TEST_SUFFIX=check .venv/bin/python .runtime/run-llm-check.py makemigrations --check --dry-run
npm --prefix miniprogram test
git diff --check
```

新检出环境可配置具有测试库创建权限的本机 PostgreSQL 后，从仓库根目录运行 `scripts/manage.sh test --noinput`；测试命令不会部署服务器。运行回归时保持 `LLM_ENABLED=0`、真实 Key 为空。

新增后端测试分布：`llm.tests` 30、`llm.tests_provider` 30、`common.tests_llm_integration` 7、`common.tests_llm_http` 2。

真实 HTTP 测试通过 `LiveServerTestCase` 启动本机 HTTP 服务，以 `urllib` 发出开发登录、multipart 上传、Bearer 会话与问答请求。识别完成结果使用明确标注的测试数据，外部模型始终替换为模拟回复，并阻止底层外网调用。

实际验证：五轮成功、第六轮 429；重放不新增任务/调用；越权 404；附图压缩与去 EXIF；删除/重登不恢复当日额度；上游响应期间删除源识别记录不恢复会话、答案和图片，已发生用量仍保留。

跨模块覆盖：账号注销、源记录删除、图片准备后失效、管理员只读权限、清理 dry-run 与真实清理、健康检查和网关状态一致。所有 provider 异常和进程超时均使用合成输入，没有把真实用户内容发给外部服务。

本机日志：`.runtime/llm-full-backend.log`、`.runtime/llm-full-frontend.log`、`.runtime/llm-http-tests.log`；临时数据库和 HTTP 测试进程由测试框架销毁。

## 第一阶段微信开发者工具验证（网关关闭）

使用 Stable 2.02.2608070、基础库 3.7.12 的 iPhone 12/13 模拟器，直接操作原生工具，API 指向本机 8000 端口。

1. 项目加载后首页展示本机模拟数据，个人中心已有开发模拟账号会话。
2. 「我的 AI 解读」显示 5/5 额度、北京时间重置、空列表及“AI 解读暂未开放”。关闭提示原先被通用说明覆盖，已修正并在工具中确认。
3. 选择已有公开雏菊样例上传到本机，真实 EfficientNet ONNX 任务完成，页面显示“候选参考：雏菊类花卉”。该样例仅验证流程，不作为新增精度证据。
4. 从真实结果进入新解读页，显示相同候选摘要、外部服务告知、5/5 额度、默认关闭的附图选项；网关关闭时同意与创建控件不可操作。未建立外部会话或发送 DeepSeek 请求。
5. 修复识别结果组件在初始无任务时传入空 Object 的告警来源；相关已有测试通过。

工具初次恢复窗口时曾记录自身 `simulator launch failed`，随后模拟器正常运行并完成上述操作；既有热重载、基础库性能/预加载提示不等同业务请求失败。此阶段未完成开启网关后的人工对话；同日后续已完成文字与附图真实调用，见 [真实联通记录](DeepSeek真实联通验证记录.md)。所有页面、真实微信授权和手机仍未全部验收。

## 启用与手工测试

1. 在本机被 Git 忽略的 `backend/.env` 填写 `DEEPSEEK_API_KEY`，设置 `LLM_ENABLED=1`、`DEEPSEEK_MODEL=deepseek-flash`，文件权限设为 `0600`。本次本机已完成配置；不要将 Key 写入小程序或提交仓库，也不要因此认为服务器已配置。
2. 重启本地 API 和 worker，使进程重新加载环境变量。本机便捷脚本为 `.runtime/local-test/停止本地测试.command`、`启动本地测试.command`；通用命令见部署 README。
3. 打开本机管理后台 →「AI 网关配置」→ 启用。软件默认每账号每天 5 轮成功、10 次提交，全站每天 200 次尝试、100 万 token；当前本机已下调为全站每天 10 次提交、100000 token、单次输出 600 token、并发 1，每账号仍为 5 个成功回合。首次两条请求期间使用更低的 2 次/日、256 输出 token 限制；后续调整没有重置已用额度。
4. 在小程序登录并完成一条花卉识别或河道观察 →「AI 解读本次识别结果」→ 阅读分享范围 → 可选附图 → 建立会话 → 主动发送问题。
5. 「我的 → 我的 AI 解读」可查看会话、额度和删除。停用网关后已有会话仍可查看，不能发送新问题。

## 边界与剩余 TODO

- [x] 有效 Key 下各一次真实文字/附图请求；回答、延迟、usage 与本地账本已核对，详见后续真实记录。
- [x] 北京 `26fe544` 源码、迁移、业务服务和关闭状态验收；未向服务器提供真实 Key，未启用 LLM 工作进程。
- [ ] 提供方控制台账单核对、完整多轮回答质量、服务器启用真实 LLM 后的运行验收。
- [ ] 手机、真实微信登录、合法请求域名与公网 HTTPS；服务器内部 HTTP 和开发者工具模拟器不替代这些验收。
- 失败不消耗成功回合，但已请求上游可能收费；未知结果按保守 token 预留记账，不自动重发。该预算只统计本平台，不包含同 Key 的其他应用，也不是人民币账单硬上限。
- 额度按平台账号，而非自然人身份计算。普通重登/换会话不重置；注销后重新注册为新账号，未额外保留身份指纹，全站匿名用量继续保留。
- 已经发送至 DeepSeek 的内容不能由本平台撤回；本地删除只清除本平台私人会话。原图通常 24 小时失效，私人对话最长随源记录保留 30 天。
- 本地 LLM、通用聊天、垃圾分类、M2 管理统计和模拟批次维护没有在本轮扩展。
