{
  "name": "hyhq-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "原生微信小程序，内置固定版本 Markdown 解析器，无需 npm 构建",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=20" }
}
